---
description: Build topic tree from a markdown document
---
Run am-build to parse a markdown document and create topic nodes from its headings.

Usage: `am-build <filepath>` — reads a markdown file, parses `#`/`##`/`###` headings, and creates topic nodes with hierarchy matching heading levels.
